# PC worker config — copy to worker.env on the owner's PC.
# The worker connects OUT to the cloud Friday, so no router/port setup here.

# Cloud Friday's WebSocket URL: ws://<your-cloud-ip>:8787
CLOUD_URL=ws://YOUR_CLOUD_IP:8787
# Same secret as BRIDGE_TOKEN on the cloud.
BRIDGE_TOKEN=
